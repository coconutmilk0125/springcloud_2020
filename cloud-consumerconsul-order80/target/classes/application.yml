server:
  port: 8082

spring:
  application:
    name: cloud-consumer-order

  cloud:
    consul:
      host: 39.102.56.173
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true

